## Getting Started
On the very first run, install the packages:

```bash
npm install
# or
yarn install
# or
pnpm install
```

To run the application, use:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
```
